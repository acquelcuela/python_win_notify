# post_images_posted

投稿済みの画像を置くディレクトリです。

`post_images/` にある画像がX投稿で使われたら、このディレクトリへ移動する運用にします。
これにより `post_images/` は「まだ使っていない画像」だけが残る状態を保ちます。

- 実際の画像ファイルはgit管理対象外です(`.gitignore`参照)。このREADMEだけがgitに残ります。
- 現時点では移動処理は未実装です(手動、または将来的に`post_x_magazine.py`側で自動化)。
